
//add receives two ints, adds them together, and returns the result
int add(int one, int two);//int add(int, int) - don't do this!

//arithmetic receives the address of threee ints, divides the value stored at the first address by
//the value stored at the second address and stores the result in the memory location allocated to the third address
//and then stores the result of modulus in the memory location allocated to the fourth address
//and then stores the result of multiplication in the memory location allocated to the fifth address
void arithmetic(int* numerator, int* denominator, int* result, int* secondResult, int* thirdResult);
